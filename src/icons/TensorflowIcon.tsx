import type { SVGProps } from "react";
import { SiTensorflow } from "react-icons/si";

const TensorflowIcon = (props: SVGProps<SVGSVGElement>) => <SiTensorflow {...props} />;

export default TensorflowIcon;
